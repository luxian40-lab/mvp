container_commands:
  01_set_client_max_body_size:
    command: |
      cat > /etc/nginx/conf.d/00_max_body_size.conf <<'EOF'
      client_max_body_size 200M;
      client_body_timeout 1200s;
      send_timeout 1200s;
      EOF
