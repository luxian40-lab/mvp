option_settings:
  aws:elasticbeanstalk:container:python:
    WSGIPath: mvp_project.wsgi:application
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: mvp_project.settings_production
    PYTHONPATH: /var/app/current:$PYTHONPATH
  # Note: staticfiles proxy settings are not supported on the Docker platform.
  # Static files should be served by the container (WhiteNoise) or configured
  # via .platform/nginx when using a custom platform.
